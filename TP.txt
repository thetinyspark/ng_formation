TP BONUS

Créer un signal privé sur AppService nommé _currentProductID<number>.
Créer un signal public sur AppService nommé currentProduct<Product>;
Créer une méthode publique sur AppService nommée setCurrentProductId, 
qui permettra de MAJ le _currentProductID ainsi que currentProduct (grâce à la réactivité)

- Faire en sorte que ProductDetailComponent utilise le signal currentProduct;

BONUS: currentProduct doit être un signal readOnly
